# impadalko.github.io (impadalko.com)

Repository for hosting my personal blog.

**Important:** Download the theme submodules after cloning the repo:
```bash
git submodule update --init --recursive

```

## Usage

### Run Locally

Start the local server with draft content enabled:

```bash
hugo server -D

```

The site will be available at `http://localhost:1313`.
